apiVersion: v1
kind: Service
metadata:
  name: svc-myobs-grafana

spec:
  type: NodePort

  selector:
    app: app-myobs-grafana

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30003
