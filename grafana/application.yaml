apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-myobs-grafana
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/raviagg/mygrafana
    targetRevision: main
    path: .

  destination:
    server: https://kubernetes.default.svc
    namespace: ns-myobs

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
