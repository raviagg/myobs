apiVersion: v1
kind: Service
metadata:
  name: svc-myobs-prometheus

spec:
  type: NodePort

  selector:
    app: app-myobs-prometheus

  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30002
